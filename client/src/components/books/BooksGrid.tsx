import React, { FC } from "react";
import styled from "styled-components";

interface BooksGridProps {}

const BooksGrid: FC<BooksGridProps> = () => {
  return <Wrapper>BooksGrid</Wrapper>;
};

const Wrapper = styled.div``;

export default BooksGrid;
