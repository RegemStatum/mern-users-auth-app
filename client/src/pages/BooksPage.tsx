import React, { FC } from "react";
import styled from "styled-components";

const BooksPage: FC = () => {
  return <Wrapper>BooksPage</Wrapper>;
};

const Wrapper = styled.div``;

export default BooksPage;
