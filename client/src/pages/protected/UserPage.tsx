import React, { FC } from "react";
import styled from "styled-components";

const UserPage: FC = () => {
  return <Wrapper>UserPage</Wrapper>;
};

const Wrapper = styled.div``;

export default UserPage;
