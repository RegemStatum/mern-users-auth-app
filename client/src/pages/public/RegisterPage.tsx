import React, { FC } from "react";
import styled from "styled-components";

const RegisterPage: FC = () => {
  return <Wrapper>RegisterPage</Wrapper>;
};

const Wrapper = styled.div``;

export default RegisterPage;
